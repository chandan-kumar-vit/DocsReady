import { View, Text } from 'react-native';
import React from 'react';

const DocViewer = ({navigation}) => {
  return (
    <View>
      <Text>Doc Viewer</Text>
    </View>
  );
};

export default DocViewer;
